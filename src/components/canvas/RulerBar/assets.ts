export const MAJOR_TICK_LENGTH_PX = 10;

export const MINOR_TICK_LENGTH_PX = 5;

export const TICK_LABEL_OFFSET_PX = 3;

/** Vertical gap between a major tick's label and the ruler bar's edge. */
export const TICK_LABEL_BASELINE_OFFSET_PX = 11;
