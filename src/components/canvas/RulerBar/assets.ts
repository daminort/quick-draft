const MAJOR_TICK_LENGTH_PX = 10;

const MINOR_TICK_LENGTH_PX = 5;

const TICK_LABEL_OFFSET_PX = 3;

/** Vertical gap between a major tick's label and the ruler bar's edge. */
const TICK_LABEL_BASELINE_OFFSET_PX = 11;

export {
  MAJOR_TICK_LENGTH_PX,
  MINOR_TICK_LENGTH_PX,
  TICK_LABEL_OFFSET_PX,
  TICK_LABEL_BASELINE_OFFSET_PX,
};
