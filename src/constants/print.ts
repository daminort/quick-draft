/** Margin (mm) around the printed/exported content. Used in: render/svg/renderDocumentToSvg.ts */
export const PAGE_MARGIN_MM = 10;

/** Fallback page size (mm), used only when the document has no printable shapes. Used in: render/svg/renderDocumentToSvg.ts */
export const EMPTY_PAGE_SIZE_MM = { width: 210, height: 297 };
